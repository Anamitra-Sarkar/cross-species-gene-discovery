# Core dependencies
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
httpx>=0.26.0
pytest>=8.0.0

# Optional: GNN (torch + torch_geometric) — for real GNN training/evaluation
# Install separately on Kaggle/Modal where GPU available:
# torch>=2.2.0
# torch_geometric>=2.4.0
# torch-scatter, torch-sparse, torch-cluster (see PyG install guide)

# Firebase auth (optional, for production auth verification)
# firebase-admin>=6.0.0
